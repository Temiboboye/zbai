import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Free Disposable Email Detector — Detect Temp Emails | ZeroBounce AI', description: 'Detect throwaway and temporary email addresses. Check if an email is from a disposable provider like Mailinator, Guerrillamail, or Tempmail.', alternates: { canonical: 'https://zerobounceai.com/free-tools/disposable-email-detector' } }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
