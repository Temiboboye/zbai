import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Free Email Pattern Generator — Find Email Formats | ZeroBounce AI', description: 'Generate likely email addresses from a name and company domain. Discover email patterns like firstname.lastname@ or first@.', alternates: { canonical: 'https://zerobounceai.com/free-tools/email-pattern-generator' } }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
