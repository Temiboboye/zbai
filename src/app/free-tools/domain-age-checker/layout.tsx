import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Free Domain Age Checker — Check Domain Trust & DNS | ZeroBounce AI', description: 'Check if a domain exists and has active DNS records. New or missing domains are high-risk for email sending.', alternates: { canonical: 'https://zerobounceai.com/free-tools/domain-age-checker' } }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
