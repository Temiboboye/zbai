import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Free MX Record Lookup — Check Domain Mail Servers | ZeroBounce AI', description: 'Check if a domain has valid mail server (MX) records. Free online MX record lookup tool.', alternates: { canonical: 'https://zerobounceai.com/free-tools/mx-record-lookup' } }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
